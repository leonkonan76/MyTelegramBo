disks:
  - name: bot-data
    mountPath: /data
    sizeGB: 50

services:
  - type: web
    name: mytelegrambot
    env: python
    plan: starter
    buildCommand: ""
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        value: votre_token_telegram_ici
      - key: ADMIN_ID
        value: votre_id_telegram_ici
    disks:
      - bot-data
